services:
  - type: web
    name: dashboard-riesgo-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    autoDeploy: true
    buildFilter:
      paths:
        - app.py
        - requirements.txt
        - render.yaml
